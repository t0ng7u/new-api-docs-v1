---
title: OAuth サードパーティログインモジュール
---

<Callout type="info" title="機能説明">
インターフェースのプレフィックスは http(s)://`<your-domain>` で統一されています
</Callout>

    本番環境では認証トークンを保護するためにHTTPSを使用する必要があります。HTTPは開発環境でのみ推奨されます。

    GitHub、OIDC、LinuxDO、WeChat、Telegramなど、複数のOAuthログイン方法をサポートしています。CSRF保護とセッション管理を実装し、アカウントの紐付けと自動登録をサポートします。フロントエンドはリダイレクト方式でOAuthフローを処理します。

## 🔐 認証不要

### GitHub OAuth リダイレクト

- **インターフェース名**：GitHub OAuth リダイレクト
- **HTTPメソッド**：GET
- **パス**：`/api/oauth/github`
- **認証要件**：公開
- **機能概要**：GitHub OAuthコールバックを処理し、ユーザーログインまたはアカウントの紐付けを完了します

💡 リクエスト例：

```
_// フロントエンドはリダイレクト方式で呼び出します。通常、GitHub OAuth認証後に自動的にコールバックされます  _window.location.href = `https://github.com/login/oauth/authorize?client_id=${github_client_id}&state=${state}&scope=user:email`;
```

✅ 成功レスポンス例：

```
{
  "success": true,
  "message": "ログイン成功",
  "data": {
    "token": "user_access_token",
    "user": {
      "id": 1,
      "username": "github_user",
      "display_name": "GitHub User",
      "email": "user@example.com"
    }
  }
}
```

❗ 失敗レスポンス例：

```
{
  "success": false,
  "message": "管理者がGitHub経由でのログインおよび登録を有効にしていません"
}
```

🧾 フィールド説明：

- `code` （文字列）: GitHub OAuth認証コード。GitHubからのコールバック時に提供されます
- `state` （文字列）: CSRF対策ステータスコード。セッションに保存されているものと一致する必要があります

### OIDC汎用OAuthリダイレクト

- **インターフェース名**：OIDC汎用OAuthリダイレクト
- **HTTPメソッド**：GET
- **パス**：`/api/oauth/oidc`
- **認証要件**：公開
- **機能概要**：OIDC OAuthコールバックを処理し、汎用OpenID Connectプロトコルログインをサポートします

💡 リクエスト例：

```
_// フロントエンドはリダイレクト方式で呼び出します  _
const url = new URL(auth_url);
url.searchParams.set('client_id', client_id);
url.searchParams.set('redirect_uri', `${window.location.origin}/oauth/oidc`);
url.searchParams.set('response_type', 'code');
url.searchParams.set('scope', 'openid profile email');
url.searchParams.set('state', state);
window.location.href = url.toString();
```

✅ 成功レスポンス例：

```
{
  "success": true,
  "message": "ログイン成功",
  "data": {
    "token": "user_access_token",
    "user": {
      "id": 1,
      "username": "oidc_user",
      "email": "user@example.com"
    }
  }
}
```

❗ 失敗レスポンス例：

```
{
  "success": false,
  "message": "OIDCユーザー情報の取得に失敗しました！設定を確認してください！"
}
```

🧾 フィールド説明：

- `code` （文字列）: OIDC認証コード
- `state` （文字列）: CSRF対策ステータスコード

### LinuxDo OAuth リダイレクト

- **インターフェース名**：LinuxDo OAuth リダイレクト
- **HTTPメソッド**：GET
- **パス**：`/api/oauth/linuxdo`
- **認証要件**：公開
- **機能概要**：LinuxDo OAuthコールバックを処理し、LinuxDoコミュニティアカウント経由でのログインをサポートします

💡 リクエスト例：

```
_// フロントエンドはリダイレクト方式で呼び出します  _
window.location.href = `https://connect.linux.do/oauth2/authorize?response_type=code&client_id=${linuxdo_client_id}&state=${state}`;
```

✅ 成功レスポンス例：

```
{
  "success": true,
  "message": "ログイン成功",
  "data": {
    "token": "user_access_token",
    "user": {
      "id": 1,
      "username": "linuxdo_user",
      "display_name": "LinuxDo User"
    }
  }
}
```

❗ 失敗レスポンス例：

```
{
  "success": false,
  "message": "管理者が新規ユーザー登録を無効にしています"
}
```

🧾 フィールド説明：

- `code` （文字列）: LinuxDo OAuth認証コード
- `state` （文字列）: CSRF対策ステータスコード
- `error` （文字列）: オプション、OAuthエラーコード
- `error_description` （文字列）: オプション、エラー説明

### WeChat QRコードログインリダイレクト

- **インターフェース名**：WeChat QRコードログインリダイレクト
- **HTTPメソッド**：GET
- **パス**：`/api/oauth/wechat`
- **認証要件**：公開
- **機能概要**：WeChat QRコードログインを処理し、認証コードを通じてログインフローを完了します

💡 リクエスト例：

```
const response = await fetch(`/api/oauth/wechat?code=${wechat_verification_code}`, {
  method: 'GET',
  headers: {
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
```

✅ 成功レスポンス例：

```
{
  "success": true,
  "message": "ログイン成功",
  "data": {
    "token": "user_access_token",
    "user": {
      "id": 1,
      "username": "wechat_user",
      "wechat_id": "wechat_openid"
    }
  }
}
```

❗ 失敗レスポンス例：

```
{
  "success": false,
  "message": "認証コードが無効または期限切れです"
}
```

🧾 フィールド説明：

`code` （文字列）: WeChat QRコードスキャンで取得した認証コード

### WeChatアカウントの紐付け

- **インターフェース名**：WeChatアカウントの紐付け
- **HTTPメソッド**：GET
- **パス**：`/api/oauth/wechat/bind`
- **認証要件**：公開
- **機能概要**：WeChatアカウントを既存のユーザーアカウントに紐付けます

💡 リクエスト例：

```
const response = await fetch(`/api/oauth/wechat/bind?code=${wechat_verification_code}`, {
  method: 'GET',
  headers: {
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
```

✅ 成功レスポンス例：

```
{
  "success": true,
  "message": "WeChatアカウントの紐付けに成功しました！"
}
```

❗ 失敗レスポンス例：

```
{
  "success": false,
  "message": "認証コードが無効またはこのWeChatアカウントは既に紐付けられています"
}
```

🧾 フィールド説明：

`code` （文字列）: WeChat QRコードスキャンで取得した認証コード

### メールアドレスの紐付け

- **インターフェース名**：メールアドレスの紐付け
- **HTTPメソッド**：GET
- **パス**：`/api/oauth/email/bind`
- **認証要件**：公開
- **機能概要**：メール認証コードを通じてメールアドレスをユーザーアカウントに紐付けます

💡 リクエスト例：

```
const response = await fetch(`/api/oauth/email/bind?email=${email}&code=${email_verification_code}`, {
  method: 'GET',
  headers: {
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
```

✅ 成功レスポンス例：

```
{
  "success": true,
  "message": "メールアカウントの紐付けに成功しました！"
}
```

❗ 失敗レスポンス例：

```
{
  "success": false,
  "message": "認証コードが無効またはメールアドレスは既に使用されています"
}
```

🧾 フィールド説明：

- `email` （文字列）: 紐付けするメールアドレス
- `code` （文字列）: メール認証コード

### Telegram ログイン

- **インターフェース名**：Telegram ログイン
- **HTTPメソッド**：GET
- **パス**：`/api/oauth/telegram/login`
- **認証要件**：公開
- **機能概要**：Telegramウィジェットを通じてユーザーログインを完了します

💡 リクエスト例：

```
const params = {
  id: telegram_user_id,
  first_name: "John",
  last_name: "Doe",
  username: "johndoe",
  photo_url: "https://...",
  auth_date: 1640995200,
  hash: "telegram_hash"
};
const query = new URLSearchParams(params).toString();
const response = await fetch(`/api/oauth/telegram/login?${query}`, {
  method: 'GET'
});
const data = await response.json();
```

✅ 成功レスポンス例：

```
{
  "success": true,
  "message": "ログイン成功",
  "data": {
    "token": "user_access_token",
    "user": {
      "id": 1,
      "username": "telegram_user",
      "telegram_id": "123456789"
    }
  }
}
```

❗ 失敗レスポンス例：

```
{
  "success": false,
  "message": "Telegram認証に失敗しました"
}
```

🧾 フィールド説明：

- `id` （文字列）: TelegramユーザーID
- `first_name` （文字列）: ユーザー名
- `last_name` （文字列）: ユーザー姓、オプション
- `username` （文字列）: Telegramユーザー名、オプション
- `photo_url` （文字列）: アバターURL、オプション
- `auth_date` （数値）: 認証タイムスタンプ
- `hash` （文字列）: Telegram認証ハッシュ

### Telegramアカウントの紐付け

- **インターフェース名**：Telegramアカウントの紐付け
- **HTTPメソッド**：GET
- **パス**：`/api/oauth/telegram/bind`
- **認証要件**：公開
- **機能概要**：Telegramアカウントを既存のユーザーアカウントに紐付けます

💡 リクエスト例：

```
// Through TelegramLoginButton component automatically handles parameters
// Parameter format is the same as Telegram login
const response = await fetch('/api/oauth/telegram/bind', {
  method: 'GET',
  params: telegram_auth_params
});
const data = await response.json();
```

✅ 成功レスポンス例：

```
{
  "success": true,
  "message": "Telegramアカウントの紐付けに成功しました！"
}
```

❗ 失敗レスポンス例：

```
{
  "success": false,
  "message": "このTelegramアカウントは既に紐付けられています"
}
```

🧾 フィールド説明：

パラメータ形式はTelegramログインインターフェースと同じです

### ランダムなstateの取得（CSRF対策）

- **インターフェース名**：ランダムなstateの取得
- **HTTPメソッド**：GET
- **パス**：`/api/oauth/state`
- **認証要件**：公開
- **機能概要**：OAuthフローのCSRF保護のためにランダムなstateパラメータを生成します

💡 リクエスト例：

```
let path = '/api/oauth/state';
let affCode = localStorage.getItem('aff');
if (affCode && affCode.length > 0) {
  path += `?aff=${affCode}`;
}
const response = await fetch(path, {
  method: 'GET',
  headers: {
    'Content-Type': 'application/json'
  }
});
const data = await response.json();
```

✅ 成功レスポンス例：

```
{
  "success": true,
  "message": "",
  "data": "random_state_string_12chars"
}
```

❗ 失敗レスポンス例：

```
{
  "success": false,
  "message": "stateの生成に失敗しました"
}
```

🧾 フィールド説明：

- `aff` （文字列）: オプション、ユーザーの出所を記録するための紹介コードパラメータ
- `data` （文字列）: 返されるランダムなstate文字列、長さは12文字です